# Environment

Current development line: `v0.1.0`

JP1-oriented operation assets are currently maintained under `ops/jp1/`.

The final JP1 environment file layout is not defined yet. Do not assume `ops/jp1/params/env` exists until a launcher implementation and environment convention are added.

When adding JP1 environment handling, update:

- `ops/jp1/README.md`
- `ops/jp1/scripts/run-job.sh`
- `docs/batch/operation-runbook.md`
