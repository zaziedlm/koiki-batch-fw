# Operation Runbook

JP1 integration artifacts are maintained under `ops/jp1/`.

Current development line: `v0.1.0`

At this stage, the JP1 assets define the initial operation contract only. They are not yet a complete production JP1 design.

Use:

- `ops/jp1/README.md` for the current JP1 scope and deferred details
- `ops/jp1/jobs/return-code-mapping.md` for return code policy
- `ops/jp1/runbook/rerun-procedure.md` for rerun guidance
- `ops/jp1/scripts/run-job.sh` for the future scheduler launcher entry point
